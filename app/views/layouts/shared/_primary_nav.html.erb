<section class="top-bar-section">
  <!-- Left Nav Section -->
  <ul class="left">
    <li class="divider"></li>
    <li class="<%= is_active?(leave_analysis_path) %>"><%= link_to "Leave Analysis", leave_analysis_path %></li>
    <li class="divider"></li>
    <li class="<%= is_active?(leave_applications_path) %>"><%= link_to "Leave Applications", leave_applications_path %></li>
    <li class="divider"></li>
    <li class="<%= is_active?(teams_path) %>"><%= link_to "Teams", teams_path %></li>
    <li class="divider"></li>
    <li class="<%= is_active?(users_path) %>"><%= link_to "Employees", users_path %></li>
    <li class="divider"></li>
  </ul>

  <!-- Right Nav Section -->
  <ul class="right">
  <li class="has-form">
      <form>
        <div class="row collapse">
          <div class="small-8 columns">
            <input type="text">
          </div>
          <div class="small-4 columns">
            <a href="#" class="alert button">Search</a>
          </div>
        </div>
      </form>
    </li>
    <li class="divider show-for-small"></li>
    <li class="has-form">
    <% unless current_user.nil? %>
      <%= link_to "Log out", destroy_session_path(current_user),method: :delete %>
    <%else %>
      <%= link_to "Log in", new_user_session_path() %>
    <% end %>
    </li>
  </ul>
</section>
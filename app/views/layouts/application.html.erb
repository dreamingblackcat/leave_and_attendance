<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
      <nav class="top-bar">
        <ul class="title-area">
          <!-- Title Area -->
          <li class="name">
            <h1><a href="#">Attendance</a></h1>
          </li>
          <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
          <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>
      <%= render partial: 'layouts/shared/primary_nav' %>
      </nav>
      <!-- TODO# Used Differenet partials here, can dry out using a helper-->
        <% if flash[:error] %>
          <%= render partial: "layouts/shared/flash_error"  %>   
        <% end %>
        <% if flash[:notice] %>
           <%= render partial: "layouts/shared/flash_notice" %>
        <% end %>
        <% if flash[:alert] %>
            <%= render partial: "layouts/shared/flash_alert" %>  
        <% end %>

    <div class="yield-content row">
        <article class="large-10 columns large-offset-1">          
          <%= yield %>
        </article>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>